// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/DouDOU-start/airgate-core/ent/modeltag"
)

// ModelTag is the model entity for the ModelTag schema.
type ModelTag struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ModelTagQuery when eager-loading is set.
	Edges        ModelTagEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ModelTagEdges holds the relations/edges for other nodes in the graph.
type ModelTagEdges struct {
	// Prices holds the value of the prices edge.
	Prices []*ModelPrice `json:"prices,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// PricesOrErr returns the Prices value or an error if the edge
// was not loaded in eager-loading.
func (e ModelTagEdges) PricesOrErr() ([]*ModelPrice, error) {
	if e.loadedTypes[0] {
		return e.Prices, nil
	}
	return nil, &NotLoadedError{edge: "prices"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ModelTag) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case modeltag.FieldID:
			values[i] = new(sql.NullInt64)
		case modeltag.FieldName:
			values[i] = new(sql.NullString)
		case modeltag.FieldCreatedAt, modeltag.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ModelTag fields.
func (mt *ModelTag) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case modeltag.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			mt.ID = int(value.Int64)
		case modeltag.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				mt.Name = value.String
			}
		case modeltag.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				mt.CreatedAt = value.Time
			}
		case modeltag.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				mt.UpdatedAt = value.Time
			}
		default:
			mt.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ModelTag.
// This includes values selected through modifiers, order, etc.
func (mt *ModelTag) Value(name string) (ent.Value, error) {
	return mt.selectValues.Get(name)
}

// QueryPrices queries the "prices" edge of the ModelTag entity.
func (mt *ModelTag) QueryPrices() *ModelPriceQuery {
	return NewModelTagClient(mt.config).QueryPrices(mt)
}

// Update returns a builder for updating this ModelTag.
// Note that you need to call ModelTag.Unwrap() before calling this method if this ModelTag
// was returned from a transaction, and the transaction was committed or rolled back.
func (mt *ModelTag) Update() *ModelTagUpdateOne {
	return NewModelTagClient(mt.config).UpdateOne(mt)
}

// Unwrap unwraps the ModelTag entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mt *ModelTag) Unwrap() *ModelTag {
	_tx, ok := mt.config.driver.(*txDriver)
	if !ok {
		panic("ent: ModelTag is not a transactional entity")
	}
	mt.config.driver = _tx.drv
	return mt
}

// String implements the fmt.Stringer.
func (mt *ModelTag) String() string {
	var builder strings.Builder
	builder.WriteString("ModelTag(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mt.ID))
	builder.WriteString("name=")
	builder.WriteString(mt.Name)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(mt.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(mt.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ModelTags is a parsable slice of ModelTag.
type ModelTags []*ModelTag
