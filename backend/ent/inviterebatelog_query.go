// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/DouDOU-start/airgate-core/ent/inviterebatelog"
	"github.com/DouDOU-start/airgate-core/ent/predicate"
)

// InviteRebateLogQuery is the builder for querying InviteRebateLog entities.
type InviteRebateLogQuery struct {
	config
	ctx        *QueryContext
	order      []inviterebatelog.OrderOption
	inters     []Interceptor
	predicates []predicate.InviteRebateLog
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the InviteRebateLogQuery builder.
func (irlq *InviteRebateLogQuery) Where(ps ...predicate.InviteRebateLog) *InviteRebateLogQuery {
	irlq.predicates = append(irlq.predicates, ps...)
	return irlq
}

// Limit the number of records to be returned by this query.
func (irlq *InviteRebateLogQuery) Limit(limit int) *InviteRebateLogQuery {
	irlq.ctx.Limit = &limit
	return irlq
}

// Offset to start from.
func (irlq *InviteRebateLogQuery) Offset(offset int) *InviteRebateLogQuery {
	irlq.ctx.Offset = &offset
	return irlq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (irlq *InviteRebateLogQuery) Unique(unique bool) *InviteRebateLogQuery {
	irlq.ctx.Unique = &unique
	return irlq
}

// Order specifies how the records should be ordered.
func (irlq *InviteRebateLogQuery) Order(o ...inviterebatelog.OrderOption) *InviteRebateLogQuery {
	irlq.order = append(irlq.order, o...)
	return irlq
}

// First returns the first InviteRebateLog entity from the query.
// Returns a *NotFoundError when no InviteRebateLog was found.
func (irlq *InviteRebateLogQuery) First(ctx context.Context) (*InviteRebateLog, error) {
	nodes, err := irlq.Limit(1).All(setContextOp(ctx, irlq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{inviterebatelog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (irlq *InviteRebateLogQuery) FirstX(ctx context.Context) *InviteRebateLog {
	node, err := irlq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first InviteRebateLog ID from the query.
// Returns a *NotFoundError when no InviteRebateLog ID was found.
func (irlq *InviteRebateLogQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = irlq.Limit(1).IDs(setContextOp(ctx, irlq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{inviterebatelog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (irlq *InviteRebateLogQuery) FirstIDX(ctx context.Context) int {
	id, err := irlq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single InviteRebateLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one InviteRebateLog entity is found.
// Returns a *NotFoundError when no InviteRebateLog entities are found.
func (irlq *InviteRebateLogQuery) Only(ctx context.Context) (*InviteRebateLog, error) {
	nodes, err := irlq.Limit(2).All(setContextOp(ctx, irlq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{inviterebatelog.Label}
	default:
		return nil, &NotSingularError{inviterebatelog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (irlq *InviteRebateLogQuery) OnlyX(ctx context.Context) *InviteRebateLog {
	node, err := irlq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only InviteRebateLog ID in the query.
// Returns a *NotSingularError when more than one InviteRebateLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (irlq *InviteRebateLogQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = irlq.Limit(2).IDs(setContextOp(ctx, irlq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{inviterebatelog.Label}
	default:
		err = &NotSingularError{inviterebatelog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (irlq *InviteRebateLogQuery) OnlyIDX(ctx context.Context) int {
	id, err := irlq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of InviteRebateLogs.
func (irlq *InviteRebateLogQuery) All(ctx context.Context) ([]*InviteRebateLog, error) {
	ctx = setContextOp(ctx, irlq.ctx, "All")
	if err := irlq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*InviteRebateLog, *InviteRebateLogQuery]()
	return withInterceptors[[]*InviteRebateLog](ctx, irlq, qr, irlq.inters)
}

// AllX is like All, but panics if an error occurs.
func (irlq *InviteRebateLogQuery) AllX(ctx context.Context) []*InviteRebateLog {
	nodes, err := irlq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of InviteRebateLog IDs.
func (irlq *InviteRebateLogQuery) IDs(ctx context.Context) (ids []int, err error) {
	if irlq.ctx.Unique == nil && irlq.path != nil {
		irlq.Unique(true)
	}
	ctx = setContextOp(ctx, irlq.ctx, "IDs")
	if err = irlq.Select(inviterebatelog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (irlq *InviteRebateLogQuery) IDsX(ctx context.Context) []int {
	ids, err := irlq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (irlq *InviteRebateLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, irlq.ctx, "Count")
	if err := irlq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, irlq, querierCount[*InviteRebateLogQuery](), irlq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (irlq *InviteRebateLogQuery) CountX(ctx context.Context) int {
	count, err := irlq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (irlq *InviteRebateLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, irlq.ctx, "Exist")
	switch _, err := irlq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (irlq *InviteRebateLogQuery) ExistX(ctx context.Context) bool {
	exist, err := irlq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the InviteRebateLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (irlq *InviteRebateLogQuery) Clone() *InviteRebateLogQuery {
	if irlq == nil {
		return nil
	}
	return &InviteRebateLogQuery{
		config:     irlq.config,
		ctx:        irlq.ctx.Clone(),
		order:      append([]inviterebatelog.OrderOption{}, irlq.order...),
		inters:     append([]Interceptor{}, irlq.inters...),
		predicates: append([]predicate.InviteRebateLog{}, irlq.predicates...),
		// clone intermediate query.
		sql:  irlq.sql.Clone(),
		path: irlq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.InviteRebateLog.Query().
//		GroupBy(inviterebatelog.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (irlq *InviteRebateLogQuery) GroupBy(field string, fields ...string) *InviteRebateLogGroupBy {
	irlq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &InviteRebateLogGroupBy{build: irlq}
	grbuild.flds = &irlq.ctx.Fields
	grbuild.label = inviterebatelog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//	}
//
//	client.InviteRebateLog.Query().
//		Select(inviterebatelog.FieldUserID).
//		Scan(ctx, &v)
func (irlq *InviteRebateLogQuery) Select(fields ...string) *InviteRebateLogSelect {
	irlq.ctx.Fields = append(irlq.ctx.Fields, fields...)
	sbuild := &InviteRebateLogSelect{InviteRebateLogQuery: irlq}
	sbuild.label = inviterebatelog.Label
	sbuild.flds, sbuild.scan = &irlq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a InviteRebateLogSelect configured with the given aggregations.
func (irlq *InviteRebateLogQuery) Aggregate(fns ...AggregateFunc) *InviteRebateLogSelect {
	return irlq.Select().Aggregate(fns...)
}

func (irlq *InviteRebateLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range irlq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, irlq); err != nil {
				return err
			}
		}
	}
	for _, f := range irlq.ctx.Fields {
		if !inviterebatelog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if irlq.path != nil {
		prev, err := irlq.path(ctx)
		if err != nil {
			return err
		}
		irlq.sql = prev
	}
	return nil
}

func (irlq *InviteRebateLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*InviteRebateLog, error) {
	var (
		nodes = []*InviteRebateLog{}
		_spec = irlq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*InviteRebateLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &InviteRebateLog{config: irlq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(irlq.modifiers) > 0 {
		_spec.Modifiers = irlq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, irlq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (irlq *InviteRebateLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := irlq.querySpec()
	if len(irlq.modifiers) > 0 {
		_spec.Modifiers = irlq.modifiers
	}
	_spec.Node.Columns = irlq.ctx.Fields
	if len(irlq.ctx.Fields) > 0 {
		_spec.Unique = irlq.ctx.Unique != nil && *irlq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, irlq.driver, _spec)
}

func (irlq *InviteRebateLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(inviterebatelog.Table, inviterebatelog.Columns, sqlgraph.NewFieldSpec(inviterebatelog.FieldID, field.TypeInt))
	_spec.From = irlq.sql
	if unique := irlq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if irlq.path != nil {
		_spec.Unique = true
	}
	if fields := irlq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, inviterebatelog.FieldID)
		for i := range fields {
			if fields[i] != inviterebatelog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := irlq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := irlq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := irlq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := irlq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (irlq *InviteRebateLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(irlq.driver.Dialect())
	t1 := builder.Table(inviterebatelog.Table)
	columns := irlq.ctx.Fields
	if len(columns) == 0 {
		columns = inviterebatelog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if irlq.sql != nil {
		selector = irlq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if irlq.ctx.Unique != nil && *irlq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range irlq.modifiers {
		m(selector)
	}
	for _, p := range irlq.predicates {
		p(selector)
	}
	for _, p := range irlq.order {
		p(selector)
	}
	if offset := irlq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := irlq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (irlq *InviteRebateLogQuery) Modify(modifiers ...func(s *sql.Selector)) *InviteRebateLogSelect {
	irlq.modifiers = append(irlq.modifiers, modifiers...)
	return irlq.Select()
}

// InviteRebateLogGroupBy is the group-by builder for InviteRebateLog entities.
type InviteRebateLogGroupBy struct {
	selector
	build *InviteRebateLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (irlgb *InviteRebateLogGroupBy) Aggregate(fns ...AggregateFunc) *InviteRebateLogGroupBy {
	irlgb.fns = append(irlgb.fns, fns...)
	return irlgb
}

// Scan applies the selector query and scans the result into the given value.
func (irlgb *InviteRebateLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, irlgb.build.ctx, "GroupBy")
	if err := irlgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InviteRebateLogQuery, *InviteRebateLogGroupBy](ctx, irlgb.build, irlgb, irlgb.build.inters, v)
}

func (irlgb *InviteRebateLogGroupBy) sqlScan(ctx context.Context, root *InviteRebateLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(irlgb.fns))
	for _, fn := range irlgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*irlgb.flds)+len(irlgb.fns))
		for _, f := range *irlgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*irlgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := irlgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// InviteRebateLogSelect is the builder for selecting fields of InviteRebateLog entities.
type InviteRebateLogSelect struct {
	*InviteRebateLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (irls *InviteRebateLogSelect) Aggregate(fns ...AggregateFunc) *InviteRebateLogSelect {
	irls.fns = append(irls.fns, fns...)
	return irls
}

// Scan applies the selector query and scans the result into the given value.
func (irls *InviteRebateLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, irls.ctx, "Select")
	if err := irls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InviteRebateLogQuery, *InviteRebateLogSelect](ctx, irls.InviteRebateLogQuery, irls, irls.inters, v)
}

func (irls *InviteRebateLogSelect) sqlScan(ctx context.Context, root *InviteRebateLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(irls.fns))
	for _, fn := range irls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*irls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := irls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (irls *InviteRebateLogSelect) Modify(modifiers ...func(s *sql.Selector)) *InviteRebateLogSelect {
	irls.modifiers = append(irls.modifiers, modifiers...)
	return irls
}
