// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/DouDOU-start/airgate-core/ent/predicate"
	"github.com/DouDOU-start/airgate-core/ent/requestauditattempt"
	"github.com/DouDOU-start/airgate-core/ent/requestauditlog"
)

// RequestAuditAttemptQuery is the builder for querying RequestAuditAttempt entities.
type RequestAuditAttemptQuery struct {
	config
	ctx         *QueryContext
	order       []requestauditattempt.OrderOption
	inters      []Interceptor
	predicates  []predicate.RequestAuditAttempt
	withRequest *RequestAuditLogQuery
	modifiers   []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RequestAuditAttemptQuery builder.
func (raaq *RequestAuditAttemptQuery) Where(ps ...predicate.RequestAuditAttempt) *RequestAuditAttemptQuery {
	raaq.predicates = append(raaq.predicates, ps...)
	return raaq
}

// Limit the number of records to be returned by this query.
func (raaq *RequestAuditAttemptQuery) Limit(limit int) *RequestAuditAttemptQuery {
	raaq.ctx.Limit = &limit
	return raaq
}

// Offset to start from.
func (raaq *RequestAuditAttemptQuery) Offset(offset int) *RequestAuditAttemptQuery {
	raaq.ctx.Offset = &offset
	return raaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (raaq *RequestAuditAttemptQuery) Unique(unique bool) *RequestAuditAttemptQuery {
	raaq.ctx.Unique = &unique
	return raaq
}

// Order specifies how the records should be ordered.
func (raaq *RequestAuditAttemptQuery) Order(o ...requestauditattempt.OrderOption) *RequestAuditAttemptQuery {
	raaq.order = append(raaq.order, o...)
	return raaq
}

// QueryRequest chains the current query on the "request" edge.
func (raaq *RequestAuditAttemptQuery) QueryRequest() *RequestAuditLogQuery {
	query := (&RequestAuditLogClient{config: raaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := raaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := raaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(requestauditattempt.Table, requestauditattempt.FieldID, selector),
			sqlgraph.To(requestauditlog.Table, requestauditlog.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, requestauditattempt.RequestTable, requestauditattempt.RequestColumn),
		)
		fromU = sqlgraph.SetNeighbors(raaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RequestAuditAttempt entity from the query.
// Returns a *NotFoundError when no RequestAuditAttempt was found.
func (raaq *RequestAuditAttemptQuery) First(ctx context.Context) (*RequestAuditAttempt, error) {
	nodes, err := raaq.Limit(1).All(setContextOp(ctx, raaq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{requestauditattempt.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (raaq *RequestAuditAttemptQuery) FirstX(ctx context.Context) *RequestAuditAttempt {
	node, err := raaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RequestAuditAttempt ID from the query.
// Returns a *NotFoundError when no RequestAuditAttempt ID was found.
func (raaq *RequestAuditAttemptQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = raaq.Limit(1).IDs(setContextOp(ctx, raaq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{requestauditattempt.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (raaq *RequestAuditAttemptQuery) FirstIDX(ctx context.Context) int {
	id, err := raaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RequestAuditAttempt entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RequestAuditAttempt entity is found.
// Returns a *NotFoundError when no RequestAuditAttempt entities are found.
func (raaq *RequestAuditAttemptQuery) Only(ctx context.Context) (*RequestAuditAttempt, error) {
	nodes, err := raaq.Limit(2).All(setContextOp(ctx, raaq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{requestauditattempt.Label}
	default:
		return nil, &NotSingularError{requestauditattempt.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (raaq *RequestAuditAttemptQuery) OnlyX(ctx context.Context) *RequestAuditAttempt {
	node, err := raaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RequestAuditAttempt ID in the query.
// Returns a *NotSingularError when more than one RequestAuditAttempt ID is found.
// Returns a *NotFoundError when no entities are found.
func (raaq *RequestAuditAttemptQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = raaq.Limit(2).IDs(setContextOp(ctx, raaq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{requestauditattempt.Label}
	default:
		err = &NotSingularError{requestauditattempt.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (raaq *RequestAuditAttemptQuery) OnlyIDX(ctx context.Context) int {
	id, err := raaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RequestAuditAttempts.
func (raaq *RequestAuditAttemptQuery) All(ctx context.Context) ([]*RequestAuditAttempt, error) {
	ctx = setContextOp(ctx, raaq.ctx, "All")
	if err := raaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RequestAuditAttempt, *RequestAuditAttemptQuery]()
	return withInterceptors[[]*RequestAuditAttempt](ctx, raaq, qr, raaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (raaq *RequestAuditAttemptQuery) AllX(ctx context.Context) []*RequestAuditAttempt {
	nodes, err := raaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RequestAuditAttempt IDs.
func (raaq *RequestAuditAttemptQuery) IDs(ctx context.Context) (ids []int, err error) {
	if raaq.ctx.Unique == nil && raaq.path != nil {
		raaq.Unique(true)
	}
	ctx = setContextOp(ctx, raaq.ctx, "IDs")
	if err = raaq.Select(requestauditattempt.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (raaq *RequestAuditAttemptQuery) IDsX(ctx context.Context) []int {
	ids, err := raaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (raaq *RequestAuditAttemptQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, raaq.ctx, "Count")
	if err := raaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, raaq, querierCount[*RequestAuditAttemptQuery](), raaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (raaq *RequestAuditAttemptQuery) CountX(ctx context.Context) int {
	count, err := raaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (raaq *RequestAuditAttemptQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, raaq.ctx, "Exist")
	switch _, err := raaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (raaq *RequestAuditAttemptQuery) ExistX(ctx context.Context) bool {
	exist, err := raaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RequestAuditAttemptQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (raaq *RequestAuditAttemptQuery) Clone() *RequestAuditAttemptQuery {
	if raaq == nil {
		return nil
	}
	return &RequestAuditAttemptQuery{
		config:      raaq.config,
		ctx:         raaq.ctx.Clone(),
		order:       append([]requestauditattempt.OrderOption{}, raaq.order...),
		inters:      append([]Interceptor{}, raaq.inters...),
		predicates:  append([]predicate.RequestAuditAttempt{}, raaq.predicates...),
		withRequest: raaq.withRequest.Clone(),
		// clone intermediate query.
		sql:  raaq.sql.Clone(),
		path: raaq.path,
	}
}

// WithRequest tells the query-builder to eager-load the nodes that are connected to
// the "request" edge. The optional arguments are used to configure the query builder of the edge.
func (raaq *RequestAuditAttemptQuery) WithRequest(opts ...func(*RequestAuditLogQuery)) *RequestAuditAttemptQuery {
	query := (&RequestAuditLogClient{config: raaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	raaq.withRequest = query
	return raaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RequestAuditID int `json:"request_audit_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RequestAuditAttempt.Query().
//		GroupBy(requestauditattempt.FieldRequestAuditID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (raaq *RequestAuditAttemptQuery) GroupBy(field string, fields ...string) *RequestAuditAttemptGroupBy {
	raaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RequestAuditAttemptGroupBy{build: raaq}
	grbuild.flds = &raaq.ctx.Fields
	grbuild.label = requestauditattempt.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RequestAuditID int `json:"request_audit_id,omitempty"`
//	}
//
//	client.RequestAuditAttempt.Query().
//		Select(requestauditattempt.FieldRequestAuditID).
//		Scan(ctx, &v)
func (raaq *RequestAuditAttemptQuery) Select(fields ...string) *RequestAuditAttemptSelect {
	raaq.ctx.Fields = append(raaq.ctx.Fields, fields...)
	sbuild := &RequestAuditAttemptSelect{RequestAuditAttemptQuery: raaq}
	sbuild.label = requestauditattempt.Label
	sbuild.flds, sbuild.scan = &raaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RequestAuditAttemptSelect configured with the given aggregations.
func (raaq *RequestAuditAttemptQuery) Aggregate(fns ...AggregateFunc) *RequestAuditAttemptSelect {
	return raaq.Select().Aggregate(fns...)
}

func (raaq *RequestAuditAttemptQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range raaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, raaq); err != nil {
				return err
			}
		}
	}
	for _, f := range raaq.ctx.Fields {
		if !requestauditattempt.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if raaq.path != nil {
		prev, err := raaq.path(ctx)
		if err != nil {
			return err
		}
		raaq.sql = prev
	}
	return nil
}

func (raaq *RequestAuditAttemptQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RequestAuditAttempt, error) {
	var (
		nodes       = []*RequestAuditAttempt{}
		_spec       = raaq.querySpec()
		loadedTypes = [1]bool{
			raaq.withRequest != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RequestAuditAttempt).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RequestAuditAttempt{config: raaq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(raaq.modifiers) > 0 {
		_spec.Modifiers = raaq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, raaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := raaq.withRequest; query != nil {
		if err := raaq.loadRequest(ctx, query, nodes, nil,
			func(n *RequestAuditAttempt, e *RequestAuditLog) { n.Edges.Request = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (raaq *RequestAuditAttemptQuery) loadRequest(ctx context.Context, query *RequestAuditLogQuery, nodes []*RequestAuditAttempt, init func(*RequestAuditAttempt), assign func(*RequestAuditAttempt, *RequestAuditLog)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*RequestAuditAttempt)
	for i := range nodes {
		fk := nodes[i].RequestAuditID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(requestauditlog.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "request_audit_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (raaq *RequestAuditAttemptQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := raaq.querySpec()
	if len(raaq.modifiers) > 0 {
		_spec.Modifiers = raaq.modifiers
	}
	_spec.Node.Columns = raaq.ctx.Fields
	if len(raaq.ctx.Fields) > 0 {
		_spec.Unique = raaq.ctx.Unique != nil && *raaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, raaq.driver, _spec)
}

func (raaq *RequestAuditAttemptQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(requestauditattempt.Table, requestauditattempt.Columns, sqlgraph.NewFieldSpec(requestauditattempt.FieldID, field.TypeInt))
	_spec.From = raaq.sql
	if unique := raaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if raaq.path != nil {
		_spec.Unique = true
	}
	if fields := raaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, requestauditattempt.FieldID)
		for i := range fields {
			if fields[i] != requestauditattempt.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if raaq.withRequest != nil {
			_spec.Node.AddColumnOnce(requestauditattempt.FieldRequestAuditID)
		}
	}
	if ps := raaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := raaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := raaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := raaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (raaq *RequestAuditAttemptQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(raaq.driver.Dialect())
	t1 := builder.Table(requestauditattempt.Table)
	columns := raaq.ctx.Fields
	if len(columns) == 0 {
		columns = requestauditattempt.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if raaq.sql != nil {
		selector = raaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if raaq.ctx.Unique != nil && *raaq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range raaq.modifiers {
		m(selector)
	}
	for _, p := range raaq.predicates {
		p(selector)
	}
	for _, p := range raaq.order {
		p(selector)
	}
	if offset := raaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := raaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (raaq *RequestAuditAttemptQuery) Modify(modifiers ...func(s *sql.Selector)) *RequestAuditAttemptSelect {
	raaq.modifiers = append(raaq.modifiers, modifiers...)
	return raaq.Select()
}

// RequestAuditAttemptGroupBy is the group-by builder for RequestAuditAttempt entities.
type RequestAuditAttemptGroupBy struct {
	selector
	build *RequestAuditAttemptQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (raagb *RequestAuditAttemptGroupBy) Aggregate(fns ...AggregateFunc) *RequestAuditAttemptGroupBy {
	raagb.fns = append(raagb.fns, fns...)
	return raagb
}

// Scan applies the selector query and scans the result into the given value.
func (raagb *RequestAuditAttemptGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, raagb.build.ctx, "GroupBy")
	if err := raagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RequestAuditAttemptQuery, *RequestAuditAttemptGroupBy](ctx, raagb.build, raagb, raagb.build.inters, v)
}

func (raagb *RequestAuditAttemptGroupBy) sqlScan(ctx context.Context, root *RequestAuditAttemptQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(raagb.fns))
	for _, fn := range raagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*raagb.flds)+len(raagb.fns))
		for _, f := range *raagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*raagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := raagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RequestAuditAttemptSelect is the builder for selecting fields of RequestAuditAttempt entities.
type RequestAuditAttemptSelect struct {
	*RequestAuditAttemptQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (raas *RequestAuditAttemptSelect) Aggregate(fns ...AggregateFunc) *RequestAuditAttemptSelect {
	raas.fns = append(raas.fns, fns...)
	return raas
}

// Scan applies the selector query and scans the result into the given value.
func (raas *RequestAuditAttemptSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, raas.ctx, "Select")
	if err := raas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RequestAuditAttemptQuery, *RequestAuditAttemptSelect](ctx, raas.RequestAuditAttemptQuery, raas, raas.inters, v)
}

func (raas *RequestAuditAttemptSelect) sqlScan(ctx context.Context, root *RequestAuditAttemptQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(raas.fns))
	for _, fn := range raas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*raas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := raas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (raas *RequestAuditAttemptSelect) Modify(modifiers ...func(s *sql.Selector)) *RequestAuditAttemptSelect {
	raas.modifiers = append(raas.modifiers, modifiers...)
	return raas
}
