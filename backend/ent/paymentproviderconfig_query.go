// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/DouDOU-start/airgate-core/ent/paymentproviderconfig"
	"github.com/DouDOU-start/airgate-core/ent/predicate"
)

// PaymentProviderConfigQuery is the builder for querying PaymentProviderConfig entities.
type PaymentProviderConfigQuery struct {
	config
	ctx        *QueryContext
	order      []paymentproviderconfig.OrderOption
	inters     []Interceptor
	predicates []predicate.PaymentProviderConfig
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PaymentProviderConfigQuery builder.
func (ppcq *PaymentProviderConfigQuery) Where(ps ...predicate.PaymentProviderConfig) *PaymentProviderConfigQuery {
	ppcq.predicates = append(ppcq.predicates, ps...)
	return ppcq
}

// Limit the number of records to be returned by this query.
func (ppcq *PaymentProviderConfigQuery) Limit(limit int) *PaymentProviderConfigQuery {
	ppcq.ctx.Limit = &limit
	return ppcq
}

// Offset to start from.
func (ppcq *PaymentProviderConfigQuery) Offset(offset int) *PaymentProviderConfigQuery {
	ppcq.ctx.Offset = &offset
	return ppcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ppcq *PaymentProviderConfigQuery) Unique(unique bool) *PaymentProviderConfigQuery {
	ppcq.ctx.Unique = &unique
	return ppcq
}

// Order specifies how the records should be ordered.
func (ppcq *PaymentProviderConfigQuery) Order(o ...paymentproviderconfig.OrderOption) *PaymentProviderConfigQuery {
	ppcq.order = append(ppcq.order, o...)
	return ppcq
}

// First returns the first PaymentProviderConfig entity from the query.
// Returns a *NotFoundError when no PaymentProviderConfig was found.
func (ppcq *PaymentProviderConfigQuery) First(ctx context.Context) (*PaymentProviderConfig, error) {
	nodes, err := ppcq.Limit(1).All(setContextOp(ctx, ppcq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{paymentproviderconfig.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ppcq *PaymentProviderConfigQuery) FirstX(ctx context.Context) *PaymentProviderConfig {
	node, err := ppcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PaymentProviderConfig ID from the query.
// Returns a *NotFoundError when no PaymentProviderConfig ID was found.
func (ppcq *PaymentProviderConfigQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ppcq.Limit(1).IDs(setContextOp(ctx, ppcq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{paymentproviderconfig.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ppcq *PaymentProviderConfigQuery) FirstIDX(ctx context.Context) int {
	id, err := ppcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PaymentProviderConfig entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PaymentProviderConfig entity is found.
// Returns a *NotFoundError when no PaymentProviderConfig entities are found.
func (ppcq *PaymentProviderConfigQuery) Only(ctx context.Context) (*PaymentProviderConfig, error) {
	nodes, err := ppcq.Limit(2).All(setContextOp(ctx, ppcq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{paymentproviderconfig.Label}
	default:
		return nil, &NotSingularError{paymentproviderconfig.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ppcq *PaymentProviderConfigQuery) OnlyX(ctx context.Context) *PaymentProviderConfig {
	node, err := ppcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PaymentProviderConfig ID in the query.
// Returns a *NotSingularError when more than one PaymentProviderConfig ID is found.
// Returns a *NotFoundError when no entities are found.
func (ppcq *PaymentProviderConfigQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ppcq.Limit(2).IDs(setContextOp(ctx, ppcq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{paymentproviderconfig.Label}
	default:
		err = &NotSingularError{paymentproviderconfig.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ppcq *PaymentProviderConfigQuery) OnlyIDX(ctx context.Context) int {
	id, err := ppcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PaymentProviderConfigs.
func (ppcq *PaymentProviderConfigQuery) All(ctx context.Context) ([]*PaymentProviderConfig, error) {
	ctx = setContextOp(ctx, ppcq.ctx, "All")
	if err := ppcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PaymentProviderConfig, *PaymentProviderConfigQuery]()
	return withInterceptors[[]*PaymentProviderConfig](ctx, ppcq, qr, ppcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ppcq *PaymentProviderConfigQuery) AllX(ctx context.Context) []*PaymentProviderConfig {
	nodes, err := ppcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PaymentProviderConfig IDs.
func (ppcq *PaymentProviderConfigQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ppcq.ctx.Unique == nil && ppcq.path != nil {
		ppcq.Unique(true)
	}
	ctx = setContextOp(ctx, ppcq.ctx, "IDs")
	if err = ppcq.Select(paymentproviderconfig.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ppcq *PaymentProviderConfigQuery) IDsX(ctx context.Context) []int {
	ids, err := ppcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ppcq *PaymentProviderConfigQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ppcq.ctx, "Count")
	if err := ppcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ppcq, querierCount[*PaymentProviderConfigQuery](), ppcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ppcq *PaymentProviderConfigQuery) CountX(ctx context.Context) int {
	count, err := ppcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ppcq *PaymentProviderConfigQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ppcq.ctx, "Exist")
	switch _, err := ppcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ppcq *PaymentProviderConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := ppcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PaymentProviderConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ppcq *PaymentProviderConfigQuery) Clone() *PaymentProviderConfigQuery {
	if ppcq == nil {
		return nil
	}
	return &PaymentProviderConfigQuery{
		config:     ppcq.config,
		ctx:        ppcq.ctx.Clone(),
		order:      append([]paymentproviderconfig.OrderOption{}, ppcq.order...),
		inters:     append([]Interceptor{}, ppcq.inters...),
		predicates: append([]predicate.PaymentProviderConfig{}, ppcq.predicates...),
		// clone intermediate query.
		sql:  ppcq.sql.Clone(),
		path: ppcq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ProviderKey string `json:"provider_key,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PaymentProviderConfig.Query().
//		GroupBy(paymentproviderconfig.FieldProviderKey).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ppcq *PaymentProviderConfigQuery) GroupBy(field string, fields ...string) *PaymentProviderConfigGroupBy {
	ppcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PaymentProviderConfigGroupBy{build: ppcq}
	grbuild.flds = &ppcq.ctx.Fields
	grbuild.label = paymentproviderconfig.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ProviderKey string `json:"provider_key,omitempty"`
//	}
//
//	client.PaymentProviderConfig.Query().
//		Select(paymentproviderconfig.FieldProviderKey).
//		Scan(ctx, &v)
func (ppcq *PaymentProviderConfigQuery) Select(fields ...string) *PaymentProviderConfigSelect {
	ppcq.ctx.Fields = append(ppcq.ctx.Fields, fields...)
	sbuild := &PaymentProviderConfigSelect{PaymentProviderConfigQuery: ppcq}
	sbuild.label = paymentproviderconfig.Label
	sbuild.flds, sbuild.scan = &ppcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PaymentProviderConfigSelect configured with the given aggregations.
func (ppcq *PaymentProviderConfigQuery) Aggregate(fns ...AggregateFunc) *PaymentProviderConfigSelect {
	return ppcq.Select().Aggregate(fns...)
}

func (ppcq *PaymentProviderConfigQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ppcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ppcq); err != nil {
				return err
			}
		}
	}
	for _, f := range ppcq.ctx.Fields {
		if !paymentproviderconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ppcq.path != nil {
		prev, err := ppcq.path(ctx)
		if err != nil {
			return err
		}
		ppcq.sql = prev
	}
	return nil
}

func (ppcq *PaymentProviderConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PaymentProviderConfig, error) {
	var (
		nodes = []*PaymentProviderConfig{}
		_spec = ppcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PaymentProviderConfig).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PaymentProviderConfig{config: ppcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ppcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ppcq *PaymentProviderConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ppcq.querySpec()
	_spec.Node.Columns = ppcq.ctx.Fields
	if len(ppcq.ctx.Fields) > 0 {
		_spec.Unique = ppcq.ctx.Unique != nil && *ppcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ppcq.driver, _spec)
}

func (ppcq *PaymentProviderConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(paymentproviderconfig.Table, paymentproviderconfig.Columns, sqlgraph.NewFieldSpec(paymentproviderconfig.FieldID, field.TypeInt))
	_spec.From = ppcq.sql
	if unique := ppcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ppcq.path != nil {
		_spec.Unique = true
	}
	if fields := ppcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, paymentproviderconfig.FieldID)
		for i := range fields {
			if fields[i] != paymentproviderconfig.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ppcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ppcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ppcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ppcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ppcq *PaymentProviderConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ppcq.driver.Dialect())
	t1 := builder.Table(paymentproviderconfig.Table)
	columns := ppcq.ctx.Fields
	if len(columns) == 0 {
		columns = paymentproviderconfig.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ppcq.sql != nil {
		selector = ppcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ppcq.ctx.Unique != nil && *ppcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ppcq.predicates {
		p(selector)
	}
	for _, p := range ppcq.order {
		p(selector)
	}
	if offset := ppcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ppcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PaymentProviderConfigGroupBy is the group-by builder for PaymentProviderConfig entities.
type PaymentProviderConfigGroupBy struct {
	selector
	build *PaymentProviderConfigQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ppcgb *PaymentProviderConfigGroupBy) Aggregate(fns ...AggregateFunc) *PaymentProviderConfigGroupBy {
	ppcgb.fns = append(ppcgb.fns, fns...)
	return ppcgb
}

// Scan applies the selector query and scans the result into the given value.
func (ppcgb *PaymentProviderConfigGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ppcgb.build.ctx, "GroupBy")
	if err := ppcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentProviderConfigQuery, *PaymentProviderConfigGroupBy](ctx, ppcgb.build, ppcgb, ppcgb.build.inters, v)
}

func (ppcgb *PaymentProviderConfigGroupBy) sqlScan(ctx context.Context, root *PaymentProviderConfigQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ppcgb.fns))
	for _, fn := range ppcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ppcgb.flds)+len(ppcgb.fns))
		for _, f := range *ppcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ppcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ppcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PaymentProviderConfigSelect is the builder for selecting fields of PaymentProviderConfig entities.
type PaymentProviderConfigSelect struct {
	*PaymentProviderConfigQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ppcs *PaymentProviderConfigSelect) Aggregate(fns ...AggregateFunc) *PaymentProviderConfigSelect {
	ppcs.fns = append(ppcs.fns, fns...)
	return ppcs
}

// Scan applies the selector query and scans the result into the given value.
func (ppcs *PaymentProviderConfigSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ppcs.ctx, "Select")
	if err := ppcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentProviderConfigQuery, *PaymentProviderConfigSelect](ctx, ppcs.PaymentProviderConfigQuery, ppcs, ppcs.inters, v)
}

func (ppcs *PaymentProviderConfigSelect) sqlScan(ctx context.Context, root *PaymentProviderConfigQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ppcs.fns))
	for _, fn := range ppcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ppcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ppcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
