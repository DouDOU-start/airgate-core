// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/DouDOU-start/airgate-core/ent/inviteprofile"
	"github.com/DouDOU-start/airgate-core/ent/predicate"
)

// InviteProfileQuery is the builder for querying InviteProfile entities.
type InviteProfileQuery struct {
	config
	ctx        *QueryContext
	order      []inviteprofile.OrderOption
	inters     []Interceptor
	predicates []predicate.InviteProfile
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the InviteProfileQuery builder.
func (ipq *InviteProfileQuery) Where(ps ...predicate.InviteProfile) *InviteProfileQuery {
	ipq.predicates = append(ipq.predicates, ps...)
	return ipq
}

// Limit the number of records to be returned by this query.
func (ipq *InviteProfileQuery) Limit(limit int) *InviteProfileQuery {
	ipq.ctx.Limit = &limit
	return ipq
}

// Offset to start from.
func (ipq *InviteProfileQuery) Offset(offset int) *InviteProfileQuery {
	ipq.ctx.Offset = &offset
	return ipq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ipq *InviteProfileQuery) Unique(unique bool) *InviteProfileQuery {
	ipq.ctx.Unique = &unique
	return ipq
}

// Order specifies how the records should be ordered.
func (ipq *InviteProfileQuery) Order(o ...inviteprofile.OrderOption) *InviteProfileQuery {
	ipq.order = append(ipq.order, o...)
	return ipq
}

// First returns the first InviteProfile entity from the query.
// Returns a *NotFoundError when no InviteProfile was found.
func (ipq *InviteProfileQuery) First(ctx context.Context) (*InviteProfile, error) {
	nodes, err := ipq.Limit(1).All(setContextOp(ctx, ipq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{inviteprofile.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ipq *InviteProfileQuery) FirstX(ctx context.Context) *InviteProfile {
	node, err := ipq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first InviteProfile ID from the query.
// Returns a *NotFoundError when no InviteProfile ID was found.
func (ipq *InviteProfileQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ipq.Limit(1).IDs(setContextOp(ctx, ipq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{inviteprofile.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ipq *InviteProfileQuery) FirstIDX(ctx context.Context) int {
	id, err := ipq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single InviteProfile entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one InviteProfile entity is found.
// Returns a *NotFoundError when no InviteProfile entities are found.
func (ipq *InviteProfileQuery) Only(ctx context.Context) (*InviteProfile, error) {
	nodes, err := ipq.Limit(2).All(setContextOp(ctx, ipq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{inviteprofile.Label}
	default:
		return nil, &NotSingularError{inviteprofile.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ipq *InviteProfileQuery) OnlyX(ctx context.Context) *InviteProfile {
	node, err := ipq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only InviteProfile ID in the query.
// Returns a *NotSingularError when more than one InviteProfile ID is found.
// Returns a *NotFoundError when no entities are found.
func (ipq *InviteProfileQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ipq.Limit(2).IDs(setContextOp(ctx, ipq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{inviteprofile.Label}
	default:
		err = &NotSingularError{inviteprofile.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ipq *InviteProfileQuery) OnlyIDX(ctx context.Context) int {
	id, err := ipq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of InviteProfiles.
func (ipq *InviteProfileQuery) All(ctx context.Context) ([]*InviteProfile, error) {
	ctx = setContextOp(ctx, ipq.ctx, "All")
	if err := ipq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*InviteProfile, *InviteProfileQuery]()
	return withInterceptors[[]*InviteProfile](ctx, ipq, qr, ipq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ipq *InviteProfileQuery) AllX(ctx context.Context) []*InviteProfile {
	nodes, err := ipq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of InviteProfile IDs.
func (ipq *InviteProfileQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ipq.ctx.Unique == nil && ipq.path != nil {
		ipq.Unique(true)
	}
	ctx = setContextOp(ctx, ipq.ctx, "IDs")
	if err = ipq.Select(inviteprofile.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ipq *InviteProfileQuery) IDsX(ctx context.Context) []int {
	ids, err := ipq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ipq *InviteProfileQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ipq.ctx, "Count")
	if err := ipq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ipq, querierCount[*InviteProfileQuery](), ipq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ipq *InviteProfileQuery) CountX(ctx context.Context) int {
	count, err := ipq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ipq *InviteProfileQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ipq.ctx, "Exist")
	switch _, err := ipq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ipq *InviteProfileQuery) ExistX(ctx context.Context) bool {
	exist, err := ipq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the InviteProfileQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ipq *InviteProfileQuery) Clone() *InviteProfileQuery {
	if ipq == nil {
		return nil
	}
	return &InviteProfileQuery{
		config:     ipq.config,
		ctx:        ipq.ctx.Clone(),
		order:      append([]inviteprofile.OrderOption{}, ipq.order...),
		inters:     append([]Interceptor{}, ipq.inters...),
		predicates: append([]predicate.InviteProfile{}, ipq.predicates...),
		// clone intermediate query.
		sql:  ipq.sql.Clone(),
		path: ipq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.InviteProfile.Query().
//		GroupBy(inviteprofile.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ipq *InviteProfileQuery) GroupBy(field string, fields ...string) *InviteProfileGroupBy {
	ipq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &InviteProfileGroupBy{build: ipq}
	grbuild.flds = &ipq.ctx.Fields
	grbuild.label = inviteprofile.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//	}
//
//	client.InviteProfile.Query().
//		Select(inviteprofile.FieldUserID).
//		Scan(ctx, &v)
func (ipq *InviteProfileQuery) Select(fields ...string) *InviteProfileSelect {
	ipq.ctx.Fields = append(ipq.ctx.Fields, fields...)
	sbuild := &InviteProfileSelect{InviteProfileQuery: ipq}
	sbuild.label = inviteprofile.Label
	sbuild.flds, sbuild.scan = &ipq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a InviteProfileSelect configured with the given aggregations.
func (ipq *InviteProfileQuery) Aggregate(fns ...AggregateFunc) *InviteProfileSelect {
	return ipq.Select().Aggregate(fns...)
}

func (ipq *InviteProfileQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ipq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ipq); err != nil {
				return err
			}
		}
	}
	for _, f := range ipq.ctx.Fields {
		if !inviteprofile.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ipq.path != nil {
		prev, err := ipq.path(ctx)
		if err != nil {
			return err
		}
		ipq.sql = prev
	}
	return nil
}

func (ipq *InviteProfileQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*InviteProfile, error) {
	var (
		nodes = []*InviteProfile{}
		_spec = ipq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*InviteProfile).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &InviteProfile{config: ipq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(ipq.modifiers) > 0 {
		_spec.Modifiers = ipq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ipq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ipq *InviteProfileQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ipq.querySpec()
	if len(ipq.modifiers) > 0 {
		_spec.Modifiers = ipq.modifiers
	}
	_spec.Node.Columns = ipq.ctx.Fields
	if len(ipq.ctx.Fields) > 0 {
		_spec.Unique = ipq.ctx.Unique != nil && *ipq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ipq.driver, _spec)
}

func (ipq *InviteProfileQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(inviteprofile.Table, inviteprofile.Columns, sqlgraph.NewFieldSpec(inviteprofile.FieldID, field.TypeInt))
	_spec.From = ipq.sql
	if unique := ipq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ipq.path != nil {
		_spec.Unique = true
	}
	if fields := ipq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, inviteprofile.FieldID)
		for i := range fields {
			if fields[i] != inviteprofile.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ipq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ipq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ipq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ipq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ipq *InviteProfileQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ipq.driver.Dialect())
	t1 := builder.Table(inviteprofile.Table)
	columns := ipq.ctx.Fields
	if len(columns) == 0 {
		columns = inviteprofile.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ipq.sql != nil {
		selector = ipq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ipq.ctx.Unique != nil && *ipq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ipq.modifiers {
		m(selector)
	}
	for _, p := range ipq.predicates {
		p(selector)
	}
	for _, p := range ipq.order {
		p(selector)
	}
	if offset := ipq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ipq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ipq *InviteProfileQuery) Modify(modifiers ...func(s *sql.Selector)) *InviteProfileSelect {
	ipq.modifiers = append(ipq.modifiers, modifiers...)
	return ipq.Select()
}

// InviteProfileGroupBy is the group-by builder for InviteProfile entities.
type InviteProfileGroupBy struct {
	selector
	build *InviteProfileQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ipgb *InviteProfileGroupBy) Aggregate(fns ...AggregateFunc) *InviteProfileGroupBy {
	ipgb.fns = append(ipgb.fns, fns...)
	return ipgb
}

// Scan applies the selector query and scans the result into the given value.
func (ipgb *InviteProfileGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ipgb.build.ctx, "GroupBy")
	if err := ipgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InviteProfileQuery, *InviteProfileGroupBy](ctx, ipgb.build, ipgb, ipgb.build.inters, v)
}

func (ipgb *InviteProfileGroupBy) sqlScan(ctx context.Context, root *InviteProfileQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ipgb.fns))
	for _, fn := range ipgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ipgb.flds)+len(ipgb.fns))
		for _, f := range *ipgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ipgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ipgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// InviteProfileSelect is the builder for selecting fields of InviteProfile entities.
type InviteProfileSelect struct {
	*InviteProfileQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ips *InviteProfileSelect) Aggregate(fns ...AggregateFunc) *InviteProfileSelect {
	ips.fns = append(ips.fns, fns...)
	return ips
}

// Scan applies the selector query and scans the result into the given value.
func (ips *InviteProfileSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ips.ctx, "Select")
	if err := ips.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InviteProfileQuery, *InviteProfileSelect](ctx, ips.InviteProfileQuery, ips, ips.inters, v)
}

func (ips *InviteProfileSelect) sqlScan(ctx context.Context, root *InviteProfileQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ips.fns))
	for _, fn := range ips.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ips.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ips.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ips *InviteProfileSelect) Modify(modifiers ...func(s *sql.Selector)) *InviteProfileSelect {
	ips.modifiers = append(ips.modifiers, modifiers...)
	return ips
}
