// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/DouDOU-start/airgate-core/ent/paymentproviderconfig"
)

// PaymentProviderConfig is the model entity for the PaymentProviderConfig schema.
type PaymentProviderConfig struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ProviderKey holds the value of the "provider_key" field.
	ProviderKey string `json:"provider_key,omitempty"`
	// Kind holds the value of the "kind" field.
	Kind string `json:"kind,omitempty"`
	// Enabled holds the value of the "enabled" field.
	Enabled bool `json:"enabled,omitempty"`
	// Config holds the value of the "config" field.
	Config map[string]string `json:"config,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PaymentProviderConfig) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case paymentproviderconfig.FieldConfig:
			values[i] = new([]byte)
		case paymentproviderconfig.FieldEnabled:
			values[i] = new(sql.NullBool)
		case paymentproviderconfig.FieldID:
			values[i] = new(sql.NullInt64)
		case paymentproviderconfig.FieldProviderKey, paymentproviderconfig.FieldKind:
			values[i] = new(sql.NullString)
		case paymentproviderconfig.FieldCreatedAt, paymentproviderconfig.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PaymentProviderConfig fields.
func (ppc *PaymentProviderConfig) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case paymentproviderconfig.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ppc.ID = int(value.Int64)
		case paymentproviderconfig.FieldProviderKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider_key", values[i])
			} else if value.Valid {
				ppc.ProviderKey = value.String
			}
		case paymentproviderconfig.FieldKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field kind", values[i])
			} else if value.Valid {
				ppc.Kind = value.String
			}
		case paymentproviderconfig.FieldEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enabled", values[i])
			} else if value.Valid {
				ppc.Enabled = value.Bool
			}
		case paymentproviderconfig.FieldConfig:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field config", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &ppc.Config); err != nil {
					return fmt.Errorf("unmarshal field config: %w", err)
				}
			}
		case paymentproviderconfig.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ppc.CreatedAt = value.Time
			}
		case paymentproviderconfig.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ppc.UpdatedAt = value.Time
			}
		default:
			ppc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PaymentProviderConfig.
// This includes values selected through modifiers, order, etc.
func (ppc *PaymentProviderConfig) Value(name string) (ent.Value, error) {
	return ppc.selectValues.Get(name)
}

// Update returns a builder for updating this PaymentProviderConfig.
// Note that you need to call PaymentProviderConfig.Unwrap() before calling this method if this PaymentProviderConfig
// was returned from a transaction, and the transaction was committed or rolled back.
func (ppc *PaymentProviderConfig) Update() *PaymentProviderConfigUpdateOne {
	return NewPaymentProviderConfigClient(ppc.config).UpdateOne(ppc)
}

// Unwrap unwraps the PaymentProviderConfig entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ppc *PaymentProviderConfig) Unwrap() *PaymentProviderConfig {
	_tx, ok := ppc.config.driver.(*txDriver)
	if !ok {
		panic("ent: PaymentProviderConfig is not a transactional entity")
	}
	ppc.config.driver = _tx.drv
	return ppc
}

// String implements the fmt.Stringer.
func (ppc *PaymentProviderConfig) String() string {
	var builder strings.Builder
	builder.WriteString("PaymentProviderConfig(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ppc.ID))
	builder.WriteString("provider_key=")
	builder.WriteString(ppc.ProviderKey)
	builder.WriteString(", ")
	builder.WriteString("kind=")
	builder.WriteString(ppc.Kind)
	builder.WriteString(", ")
	builder.WriteString("enabled=")
	builder.WriteString(fmt.Sprintf("%v", ppc.Enabled))
	builder.WriteString(", ")
	builder.WriteString("config=")
	builder.WriteString(fmt.Sprintf("%v", ppc.Config))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ppc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ppc.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PaymentProviderConfigs is a parsable slice of PaymentProviderConfig.
type PaymentProviderConfigs []*PaymentProviderConfig
