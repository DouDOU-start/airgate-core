// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/DouDOU-start/airgate-core/ent/redemptioncode"
)

// RedemptionCode is the model entity for the RedemptionCode schema.
type RedemptionCode struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Code holds the value of the "code" field.
	Code string `json:"code,omitempty"`
	// 面值（元），兑换后 1:1 计入余额
	Value float64 `json:"value,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// 批次备注（同一次生成的码共享同一备注，兼作批次标识）
	Remark string `json:"remark,omitempty"`
	// 兑换人用户 ID 快照，0 表示未使用
	UsedByID int `json:"used_by_id,omitempty"`
	// UsedByEmail holds the value of the "used_by_email" field.
	UsedByEmail string `json:"used_by_email,omitempty"`
	// UsedAt holds the value of the "used_at" field.
	UsedAt *time.Time `json:"used_at,omitempty"`
	// 过期时间，NULL 表示永不过期
	ExpiresAt *time.Time `json:"expires_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RedemptionCode) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case redemptioncode.FieldValue:
			values[i] = new(sql.NullFloat64)
		case redemptioncode.FieldID, redemptioncode.FieldUsedByID:
			values[i] = new(sql.NullInt64)
		case redemptioncode.FieldCode, redemptioncode.FieldStatus, redemptioncode.FieldRemark, redemptioncode.FieldUsedByEmail:
			values[i] = new(sql.NullString)
		case redemptioncode.FieldUsedAt, redemptioncode.FieldExpiresAt, redemptioncode.FieldCreatedAt, redemptioncode.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RedemptionCode fields.
func (rc *RedemptionCode) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case redemptioncode.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			rc.ID = int(value.Int64)
		case redemptioncode.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				rc.Code = value.String
			}
		case redemptioncode.FieldValue:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field value", values[i])
			} else if value.Valid {
				rc.Value = value.Float64
			}
		case redemptioncode.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				rc.Status = value.String
			}
		case redemptioncode.FieldRemark:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field remark", values[i])
			} else if value.Valid {
				rc.Remark = value.String
			}
		case redemptioncode.FieldUsedByID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field used_by_id", values[i])
			} else if value.Valid {
				rc.UsedByID = int(value.Int64)
			}
		case redemptioncode.FieldUsedByEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field used_by_email", values[i])
			} else if value.Valid {
				rc.UsedByEmail = value.String
			}
		case redemptioncode.FieldUsedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field used_at", values[i])
			} else if value.Valid {
				rc.UsedAt = new(time.Time)
				*rc.UsedAt = value.Time
			}
		case redemptioncode.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				rc.ExpiresAt = new(time.Time)
				*rc.ExpiresAt = value.Time
			}
		case redemptioncode.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				rc.CreatedAt = value.Time
			}
		case redemptioncode.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				rc.UpdatedAt = value.Time
			}
		default:
			rc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// GetValue returns the ent.Value that was dynamically selected and assigned to the RedemptionCode.
// This includes values selected through modifiers, order, etc.
func (rc *RedemptionCode) GetValue(name string) (ent.Value, error) {
	return rc.selectValues.Get(name)
}

// Update returns a builder for updating this RedemptionCode.
// Note that you need to call RedemptionCode.Unwrap() before calling this method if this RedemptionCode
// was returned from a transaction, and the transaction was committed or rolled back.
func (rc *RedemptionCode) Update() *RedemptionCodeUpdateOne {
	return NewRedemptionCodeClient(rc.config).UpdateOne(rc)
}

// Unwrap unwraps the RedemptionCode entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rc *RedemptionCode) Unwrap() *RedemptionCode {
	_tx, ok := rc.config.driver.(*txDriver)
	if !ok {
		panic("ent: RedemptionCode is not a transactional entity")
	}
	rc.config.driver = _tx.drv
	return rc
}

// String implements the fmt.Stringer.
func (rc *RedemptionCode) String() string {
	var builder strings.Builder
	builder.WriteString("RedemptionCode(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rc.ID))
	builder.WriteString("code=")
	builder.WriteString(rc.Code)
	builder.WriteString(", ")
	builder.WriteString("value=")
	builder.WriteString(fmt.Sprintf("%v", rc.Value))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(rc.Status)
	builder.WriteString(", ")
	builder.WriteString("remark=")
	builder.WriteString(rc.Remark)
	builder.WriteString(", ")
	builder.WriteString("used_by_id=")
	builder.WriteString(fmt.Sprintf("%v", rc.UsedByID))
	builder.WriteString(", ")
	builder.WriteString("used_by_email=")
	builder.WriteString(rc.UsedByEmail)
	builder.WriteString(", ")
	if v := rc.UsedAt; v != nil {
		builder.WriteString("used_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := rc.ExpiresAt; v != nil {
		builder.WriteString("expires_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(rc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(rc.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RedemptionCodes is a parsable slice of RedemptionCode.
type RedemptionCodes []*RedemptionCode
